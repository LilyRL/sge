[package]
name = "sge_debugging"
version = "1.0.0"
edition = "2024"

[dependencies]
fps_ticker.workspace = true
sge_global.workspace = true
log.workspace = true

[features]
default = ["debugging"]
debugging = []
